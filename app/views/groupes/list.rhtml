<h1>Liste des groupes</h1>

<%= show_pages_links(@groupe_pages, 'un groupe') %>

<% titres = ['Nom', 'Logiciels']%>
<%= show_table(@groupes, Groupe, titres) { |g|
  result = ''
  result << "<td>#{link_to g.nom, :action => 'show', :id => g.id }</td>"
  result << "<td>#{g.logiciels.compact.size}</td>"
  result << "#{link_to_actions_table g}"
}%>


<%= show_pages_links(@groupe_pages, 'un groupe') %>

