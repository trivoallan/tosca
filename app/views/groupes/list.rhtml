<h1>Liste des groupes</h1>

<%= show_pages_links(@groupe_pages, 'un groupe') %>

<% titres = []%>
<%= show_table(@groupes, Groupe, titres, :content_columns => true) { |c|
  result = ''
  result << "#{link_to_actions_table c}"
}%>


<%= show_pages_links(@groupe_pages, 'un groupe') %>

