<h1><%= _("Update the request %s (administration)") % @request_tosca.id %>
    <%= StaticImage::spinner %></h1>

<% form_tag(request_path(@request_tosca.id), {:method => :put, :multipart => true,
              :id => 'main_form', :name => 'main_form' }) do %>
  <%= render :partial => 'form' %>
  <%= submit_tag _("Update") %>
<% end %>

<%= link_to_back %>
