<h1><%= _("List of the possible contractual commitments of a contract") %></h1>


<%= show_pages_links(@engagement_pages, _('a possible commitment')) %>

<%= show_table_engagements @engagements %>

<%= show_pages_links(@engagement_pages, _('a possible commitment')) %>

