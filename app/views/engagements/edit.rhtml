<h1><%=_('Editing a commitment')%></b></h1>

<% form_tag engagement_path(@engagement), :method => :put do %>
  <%= render :partial => 'form' %>
  <%= submit_tag 'Validate' %>
<% end %>

<%= link_to 'Show', engagement_path(@engagement) %> |
<%= link_to_back %>
