<% for column in Machine.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%= @machine.send(column.name) %>
</p>
<% end %>

<p>
<b><%= _('System') << ' :'%></b> <%= @machine.socle.nom if @machine.socle %>
</p>


<%= link_to _('Edit'), edit_machine_path(@machine) %> |
<%= link_to _('Back'), machines_path %>
