<div class="show">
<% for column in Machine.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%= @machine.send(column.name) %>
</p>
<% end %>

<p>
<b><%= _('System') + ' :'%></b> <%= @machine.socle.name if @machine.socle %>
</p>
</div>
<br />

<%= link_to_edit_and_list @machine %>
