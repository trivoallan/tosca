<h1><%= _('Skill list')%></h1>

<% paginated_section(@competences, :create_label => _('a skill')) do %>
 <% titles = [ _('Resources')]%>
 <%= show_table(@competences, Competence, titles, :content_columns => true) do |c|
   result = ''
   result << "<td>#{c.ingenieurs.count}</td>"
   result << "#{link_to_actions_table c}"
 end %>
 <%= page_entries_info @competences %><br />
<% end %>
