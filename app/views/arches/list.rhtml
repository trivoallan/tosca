<h1>Liste des architectures processeurs</h1>

<%= show_pages_links(@arch_pages, 'une architecture processeur') %>

<% titres = []%>
<%= show_table(@arches, Arch, titres, :content_columns => true) { |c|
  result = ''
  result << "#{link_to_actions_table c}"
}%>


<%= show_pages_links(@arch_pages, 'une architecture processeur') %>


