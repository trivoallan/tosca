<h1>Liste des correctifs</h1>

<%= show_pages_links(@correctif_pages) %>

<% titres = ['Màj']%>
<%= show_table(@correctifs, Correctif, titres, :content_columns => true) { |c|
  result = ''
  result << "<td>#{c.updated_on.strftime("%d/%m/%Y à %H:%M:%S")}</td>"
  result << "#{link_to_actions_table c}"
}%>


<%= show_pages_links(@correctif_pages) %>



