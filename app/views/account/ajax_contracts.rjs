if @contrats
  page.replace_html("contracts",
      hbtm_check_box( @user.contrats, @contrats, 'user[contrat_ids]', :size => 2))
else
  page.replace_html("contracts", _('No contracts currently open.'))
end
