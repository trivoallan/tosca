if @contracts && !@contracts.empty?
  page.replace_html("contracts",
      hbtm_check_box( @user.contracts, @contracts, 'user[contract_ids]', :size => 2))
else
  page.replace_html("contracts", _('No contracts currently open.'))
end
