<h1><%= _("List of images") %></h1>

<% links = show_pages_links(@image_pages, _("an image")) %>
<%= links %>

<% titres = [_("Formatted"), _("Description")]%>
<%= show_table(@images, Image, titres) { |p|
  result = ''
  result << "<td>#{self.image_tag(url_for_file_column(p, 'image', 'thumb'))}</td>"
  result << "<td>#{p.description}</td>"
  result << "#{link_to_actions_table(p)}"
}%>

<%= links %>
