<h1>Editing image</h1>

<% form_tag (image_path(@image), {:method => :put} , {:multipart => true}) do %>
  <%= render :partial => 'form' %>
  <%= submit_tag 'Edit' %>
<% end %>

<%= link_to 'Show', image_path(@image) %> |
<%= link_to_back %>
