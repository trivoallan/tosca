<% logiciel = @version.logiciel %>
<%= public_link_to _('Homepage') , welcome_url%> >
 <%= public_link_to _('Software') , logiciels_path %> >
  <%= public_link_to logiciel.groupe.name, logiciel_path(logiciel.groupe_id) %> >
   <%= public_link_to logiciel.name, logiciel_path(logiciel) %> >
    <%=@version.version%><%="-#{@version.release}" unless @version.release.blank? %>
     <%= link_to_edit(@version) %>

   <h2><%= _('Source package')%></h2>

<% unless @version.active %>
<div class="show">
  <p><%= _('<b>Note : </b>This package isn\'t from now on your perimeter.
  It is preserved here only for historical reasons.')%></p>
 </div>
<% end %>

<table class="topbox">
  <tr><td>
   <div class="show" style="min-width: 30em;">
    <p><b><%= _('Software') + ' : '%></b> <%= @version.logiciel.name %></p>
    <p><b><%= _('Contract') + ' : '%></b> <%= @version.contract.client.name %></p>

    <% for column in Paquet.content_columns %>
     <p><b><%= _(column.human_name) %>:</b> <%= @version.send(column.name) %></p>
    <% end %>

    <p><b><%= _('Container') + ' : '%></b> <%= @version.conteneur.name %></p>
    
    <% unless @version.distributeur.nil? %>
    <p><b><%= _('Distributor')+ ' : '%></b> <%=h @version.distributeur.name %></p>
    <% end %>
    
    <% unless @version.mainteneur.nil? %>
    <p><b><%= _('Maintainer')+' : '%></b> <%=h @version.mainteneur.name %></p>
    <% end %>
   </div>
   <p><b><%= pluralize(@version.binaires.count, _('binary') ) %></b> <%= link_to_new_binaire(@version.id) %></p>
    <% options = {:no_title => true, :edit => 'binaire'} %>
   <p>
     <% if @version.binaires.size > 0 %>
        <%= show_list(@binaires, _('binary'), options) {|b|
            link_to_binaire(b) << _(' <i>on</i> ') << b.socle.name
          } %>
     <% end %>
   </p>
  </td><td>
   <% unless @beneficiaire or @version.configuration.blank? %>
   <div class="info">
    <fieldset>
    <legend><%=_('Configuration')%></legend>
     <%= @version.configuration %>
    </fieldset>
   </div>
   <% end %>
  </td></tr>
</table>

<br />
<% unless @changelogs.empty? %>
<p><%= _('This package has been updated')%><b><%= pluralize(@changelogs.size, _('times') ) %></b></p>
 <div class="bloc_scroll">
   <%= show_list(@changelogs, _('changelog'), :no_title => true) { |c|
      indent(c.to_s) } %>
 </div>
<% end %>


<p>&nbsp;</p>

<%= link_to_edit_and_list(@version) %>
