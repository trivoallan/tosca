Bonjour,<br/>

<% if @statut_modifie %>
  <p>
    <% url = url_for :only_path => false, :id => @demande.id, :controller => 'demandes', :action => 'comment' %>
&nbsp;&nbsp;<%=h @nom %> a passé votre <%= "<a href=\"#{url}\">demande ##{@demande.id}.</a>" %><br/> en <%= @demande.statut.nom %> :
  </p>
<% else %>
  <p>
&nbsp;&nbsp;Votre demande a été commentée par <%=h @nom %> :
  </p>
<% end %>

<%= @commentaire.corps %>

<%= render :partial => '../notifier/end_mail' %>
