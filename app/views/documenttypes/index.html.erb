<h1><%= _("List of the types of documents") %></h1>

<% titres = [] %>
<%= show_table(@documenttypes, Documenttype, titres, :content_columns => true) { |m|
  result = ''
  result << "#{link_to_actions_table m}"
}%>
