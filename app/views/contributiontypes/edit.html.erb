<h1><%= t("Editing a type of contribution") %></h1>

<% form_tag contributiontype_path(@contributiontype), :method => :put do %>
  <%= render :partial => 'form' %>
  <%= submit_tag t("Edit") %>
<% end %>

<%= link_to t("Show"), contributiontype_path(@contributiontype) %> |
<%= link_to_back %>
