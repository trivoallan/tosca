<div class="issue_tab_content" name="issue_tab_content">

<% commitment = @issue.commitment %>
<% elapsed = @issue.elapsed %>
<% interval = @issue.interval %>
<% if commitment.nil? %>
  <p><%= t("Your issue is not subject to a SLA.") %></p>
<% else %>
 <h1><%= t('Service Level Agreement') %></h1>
 <table class="full">
  <thead>
   <tr>
    <th></th>
    <th><%= t('Step') %></th>
    <th><%= t('Progress') %></th>
    <th><%= t('Commitment') %></th>
   </tr>
   <tr><td colspan="4"><hr /></td></tr>
  </thead><tbody>
   <% progress = elapsed.taken_into_account_progress %>
   <tr class="<%= cycle('even', 'odd') %>">
    <td><%= progress_image(elapsed.taken_into_account?, progress) %></td>
    <td><strong><%= t("Taken into account") %></strong></td>
    <td><%= progress_bar(progress) %></td>
    <td><%= progress_text(elapsed.taken_into_account, 1.hour, interval) %></td>
   </tr>
   <% progress = elapsed.workaround_progress %>
   <tr class="<%= cycle('even', 'odd') %>">
    <% if commitment.workaround <= 0 %>
     <td colspan="4"><%= t("Your issue has no commitment for workaround time") %></td>
    <% else %>
     <td><%= progress_image(elapsed.workaround?, progress) %></td>
     <td><strong><%= t("Workaround") %></strong></td>
     <td><%= progress_bar(elapsed.workaround_progress) %></td>
     <td><%= progress_text(elapsed.workaround, commitment.workaround.days, interval) %></td>
    <% end %>
   </tr>
  <% progress = elapsed.correction_progress %>
   <tr class="<%= cycle('even', 'odd') %>">
    <% if commitment.correction <= 0 %>
     <td colspan="4"><%= t("Your issue has no commitment for correction time") %></td>
    <% else %>
     <td><%= progress_image(elapsed.correction?, progress) %></td>
     <td><strong><%= t("Correction") %></strong></td>
     <td><%= progress_bar(progress) %></td>
     <td><%= progress_text(elapsed.correction, commitment.correction.days, @issue.interval) %></td>
    <% end %>
   </tr>
  </tbody>
 </table>
<% end %>
</div>
