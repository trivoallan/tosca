<h1><%= _("Submit a request") %> <%= StaticImage::spinner %></h1>

<h1>TODO : un seul bouton, on passe en mode toujours en faire une
  autre.</h1>
<h1>Lors des autres dépots, afficher les liens de consultations, avec
  un target=_blank (nouvelle fenetre) </h1>



<p>&nbsp;</p>
<p><%= _("Inform the fields below.") %></p>
<p><%= _("One of our consultants will then contact you in less than one hour.") %></p>
<p>&nbsp;</p>


<% form_tag( demandes_path, {:multipart => true, :id => 'demande_form',
             :name => 'demande_form' }) do %>
  <%= render :partial => 'form' %>
  <%= submit_tag _("Submit this request") %>&nbsp;
  <%= submit_tag(_("Submit this request and make an other"),
                 'onclick' => 'this.form.similar_request.value=1;this.form.submit();') %>
<% end %>

<p>&nbsp;</p>

<%= link_to_back %>

<p>&nbsp;</p>

<% if false #deactivated, for now %>
<% client = @beneficiaire.client %>
<p><%= _("Nota : One day work corresponds to the time of opening of the daily service defined in the OSSA contract %s : %s, that is to say %s hours.") % [ client.name, client.support.name, client.support.interval ] %>
<br /><%= _("The working days are from Monday to Friday, except weekends and public holidays.") %></p>
<% end %>
