<h1><%= _("Update the request %s (administration)") % @demande.id %>
    <%= StaticImage::spinner %></h1>

<% form_tag(demande_path(@demande.id), {:method => :put, :multipart => true,
              :id => 'main_form', :name => 'main_form' }) do %>
  <%= render :partial => 'form' %>
  <%= submit_tag _("Update") %>
<% end %>

<%= link_to_back %>
