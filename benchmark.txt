Pour faire un benchmark, il faut se logger, récupérer son cookie de
session et taper la ligne magique suivante :
/usr/sbin/ab -C '_session=???'-c 100 -n 1000 http://www2:3001/:controller/:action/:id
test
