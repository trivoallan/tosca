#Réattribuer toutes les demandes d'un bénéficiaire à un autre :
# id_old : id de l'ancien l'identifiant 
# id_new : id du nouveau identifiant

new_benef_id = Beneficiaire.find_by_identifiant_id(id_new).id

Demande.find_by_beneficiaire_id(Beneficiaire.find_by_identifiant_id(id_old).id).each do |d|
	d.beneficiaire_id = new_benef_id
	d.save
end

Ou plus simple :

Demande.find_all_by_beneficiaire_id(ancien_benef_id).each do |d| d.beneficiaire_id = new_benef_id; d.save; end

