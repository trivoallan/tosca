#Réattribuer toutes les demandes d'un bénéficiaire à un autre :
# id_old : id de l'ancien l'identifiant 
# id_new : id du nouveau identifiant

new_benef_id = Recipient.find_by_identifiant_id(id_new).id

Demande.find_by_recipient_id(Recipient.find_by_identifiant_id(id_old).id).each do |d|
	d.recipient_id = new_benef_id
	d.save
end

Ou plus simple :

Demande.find_all_by_recipient_id(ancien_benef_id).each do |d| d.recipient_id = new_benef_id; d.save; end

